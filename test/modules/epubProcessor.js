const assert = require('assert')
// import { EpubProcessor } from '@modules/epubProcessor'
import ZipStream from 'node-stream-zip'
import { getTestFilePath } from '@testHelpers/getFile'

describe('epubProcessor.js', () => {
    it('test our shit', async () => {
        // const processor = new EpubProcessor(getTestFilePath('no-cover.epub'))

        // expect(true).toBe(true)
        // await expectAsync(processor.parseMetadata()).toBeResolved()
    })
})